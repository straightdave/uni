<VirtualHost *:{port}>
    ServerAdmin webmaster@localhost
    DocumentRoot {docroot}

    <Directory {docroot}>
        AllowOverride All
        Order allow,deny
        allow from all
    </Directory>
</VirtualHost>
